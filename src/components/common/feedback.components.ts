import { Alert, FormHelperText, styled } from "@mui/material";

export const FormAlertComponent = styled(Alert)({ padding: 0 });

export const FormErrorMessageComponent = styled(FormHelperText)({
  color: "#dc3545",
});
